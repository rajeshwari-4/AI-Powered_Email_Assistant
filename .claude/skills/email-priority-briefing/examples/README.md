# Examples

This directory holds sample briefings once this skill has been run against a real Gmail inbox, one per cadence (Daily/Weekly/Monthly) is useful since each has a different date-range shape. It is currently empty — populate it after production runs by saving each generated HTML briefing here as a reference case for future comparison.
